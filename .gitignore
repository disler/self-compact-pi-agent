# previous implementation versions kept out of the repo view
tmp/
# runtime artifacts written by just verify / the e2e suites (recreated on every run)
apps/self-compact/verification/
# tsconfig with machine-specific paths to Pi's types, generated by scripts/typecheck.sh
apps/self-compact/tsconfig.tsc.json
node_modules/
.env
.DS_Store
